# Project-wide Python dependencies for SmartAttendanceSystem
# Install with: python -m pip install -r requirements.txt

# FastAPI web apps
fastapi
uvicorn[standard]

# Database and ORM
SQLAlchemy
psycopg2-binary
passlib[bcrypt]

# FastAPI extras

pydantic
python-multipart
Jinja2

# HTTP / utilities
requests

# Kivy GUI and extensions
kivy
kivymd

# Computer vision / face recognition
opencv-python
deepface
mtcnn
joblib
tensorflow

# Scientific / plotting
numpy
matplotlib
seaborn
plotly
Pillow

# Misc
python-dotenv

# Notes:
# - Installing `tensorflow` on Windows may require choosing the appropriate
#   CPU/GPU wheel for your system. If you only need DeepFace for small tests,
#   consider `pip install tensorflow-cpu` to avoid GPU toolchain issues.from fastapi import FastAPI, Form, HTTPException
